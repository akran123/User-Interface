# 1. 일단 모든 파일을 무시합니다.
*

# 2. .gitignore 파일 자체는 무시하면 안 되니까 살립니다.
!.gitignore

# 3. 메인 파일 살리기
!main.py

# 4. 라우터 폴더(또는 파일) 살리기
# 만약 폴더 이름이 'routers'라면 아래 두 줄을 쓰세요.
!router/
!router/**

# (혹시 폴더가 아니라 router.py 파일 하나라면 '!router.py' 라고 쓰세요)